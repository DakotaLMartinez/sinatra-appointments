<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Sinatra Appointments App</title>
    <link rel="stylesheet" href="<%=url("/css/style.css")%>"></link>
  </head>
  <body>
    <%= erb :_mainmenu %>
    <%= yield %>
    <% if logged_in? %>
    <div class="container">Current User: <%= current_user.username %>, <%= current_user.role.name  %></div>
    <% end %>
  </body>
</html>